# the-machine-learning-brick

# PlantUML generieren

`plantuml -o generated docs/uml/*.puml`

# Dateistruktur

```
project-root
│
├── docs                    <= Enthält die Projektdokumentation
│   └── uml                 <= Enthält UML-Diagramme, die das System beschreiben
│
└──

```
